<UserControl x:Class="TestProject3.View.LineLoadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestProject3.View"
             xmlns:viewModel="clr-namespace:TestProject3.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <Rectangle x:Name="OutterRec" Width="{Binding MaxValue}" Height="100" Fill="#FFBB1B1B" >
                   
            </Rectangle>
            <Rectangle Height="{Binding ElementName=OutterRec, Path=Height}" Width="{Binding Value}" Fill="Blue">
                <Rectangle.RenderTransform>
                    <TranslateTransform Y="-100"/>
                </Rectangle.RenderTransform>
            </Rectangle>
        </StackPanel>
    </Grid>
</UserControl>
